import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "placehold.co",
      "dummyjson.com",
      "cdn.dummyjson.com",
      "image01.realme.net",
      "files.edgestore.dev",
    ],
  },
};

export default nextConfig;
